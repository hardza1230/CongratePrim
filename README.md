# งบซอง — Envelope Budgeting

แอปมือถือ (PWA) สำหรับทำงบประมาณแบบซอง แปลงมาจากไฟล์ Google Sheet วางแผนการเงินครัวเรือน
ทำงานได้ทั้งออนไลน์และออฟไลน์ ไม่ต้องติดตั้งอะไร ไม่ต้อง build

## แอปนี้ทำอะไร

ตอบคำถามเดียว: **สิ้นเดือนนี้จะเหลือเงินเท่าไหร่**

```
เงินสดตอนนี้ − ที่ยังไม่ได้จ่ายของเดือนนี้ = สิ้นเดือนจะเหลือ
```

net worth ตอบว่า "รวยแค่ไหน" แต่คำถามที่ใช้ตัดสินใจจริงคือ "เดือนนี้จะรอดไหม"
หนี้บ้านก้อนใหญ่ทำให้ตัวเลขสุทธิกลบเงินที่ใช้ได้จริงจนมองไม่เห็น แอปนี้จึงยึดกระแสเงินสดเป็นหลัก

### หน้าแรก — checklist ของเดือน

- **ต้องจ่ายประจำ** — ค่าบ้าน ค่ารถ ค่าหอ ฯลฯ แตะติ๊กเมื่อจ่ายแล้ว ยอดจะหายออกจาก "ยังไม่ได้จ่าย"
  รายการที่ผูกกับบัญชีหนี้ ใส่ได้ว่าจ่ายเป็น **เงินต้น** เท่าไหร่ **ดอกเบี้ย** เท่าไหร่
- **มีงบไว้ใช้** — ค่าอาหาร ค่าน้ำมัน ตั้งงบไว้แล้วใส่ยอดที่ใช้ไป
  แถวจะบอกว่า **เหลือให้ใช้อีกเท่าไหร่**

### นับเงิน — 2 ขั้น

1. ใส่ยอดคงเหลือจริงของแต่ละบัญชี (รวมยอดหนี้คงเหลือของบัญชีประเภทหนี้)
2. ติ๊กว่าจ่ายอะไรไปแล้วบ้าง — เป็น checklist ชุดเดียวกับหน้าแรก

บันทึกแล้วได้ snapshot หนึ่งจุด ทำหลายครั้งก็ต่อกันเป็น **กราฟเงินสดย้อนหลัง** บนหน้าแรก

> ยอดที่นับเป็นตัวจริงเสมอ — เงินต้น/ดอกเบี้ยที่กรอกไว้เป็นข้อมูลอธิบายว่าเงินที่จ่ายไปเป็นค่าอะไร
> ไม่ได้เอาไปหักยอดหนี้ซ้ำ

## ความสามารถ

| หน้า | ทำอะไรได้ |
|---|---|
| 🏠 **หน้าแรก** | สิ้นเดือนจะเหลือเท่าไหร่ · เงินสดตอนนี้ · ยังไม่ได้จ่าย · ปุ่มนับเงิน · checklist เดือนนี้ (เพิ่ม/แก้/ลบรายการได้) · กราฟเงินสดย้อนหลัง (สลับดูเป็นตัวเลขได้) · ประวัติการนับ |
| 🏦 **บัญชี** | บัญชีแยกตามเจ้าของพร้อมตราธนาคาร · **บัญชีประเภทหนี้สิน** · สลับ "นับเป็นเงินสด" และ "นับบัญชีนี้ตอนนับเงิน" · จัดลำดับ ▲▼ · เพิ่ม-แก้-ลบเจ้าของ · สรุปสินทรัพย์/หนี้สิน/สุทธิ |

รายละเอียดอื่น ๆ

- **เงินสด** นับเฉพาะบัญชีที่ตั้งว่า "นับเป็นเงินสด" — ลูกหนี้และบัญชีหนี้ไม่ถูกนับโดยปริยาย
- **ตราธนาคาร** รองรับ SCB · KBANK · KTB · BBL · BAY · ttb · GSB · KKP · ธ.ก.ส. · ธอส. · UOB · CIMB ·
  TISCO · LH · TrueMoney · ShopeePay · Rabbit LINE Pay
  สีเป็นค่าประมาณของสีแบรนด์ ไม่ใช่ค่าสีทางการ — ปรับเองได้ทุกบัญชีในช่อง "สีเอง"
- **สลับเดือนได้** ที่แถบด้านบน — checklist แยกกันตามเดือน ส่วนยอด/งบที่ตั้งไว้ยกมาทุกเดือน
- **กราฟทำจาก SVG เขียนเอง** ไม่พึ่งไลบรารี แอปจึงยังเปิดออฟไลน์ได้
- **โหมดสว่าง/มืด** ตามระบบ หรือกดปุ่ม 🌗 มุมขวาบนเพื่อสลับเอง
- ตัวเลขเงินเป็นบาท ปีแสดงเป็น พ.ศ.
- ข้อมูลเก่าจากเวอร์ชันก่อนถูกอัปให้เข้าโครงใหม่อัตโนมัติ (ซองเดิม → รายการ, หนี้สินเดิม → บัญชีประเภทหนี้)

## ค่าเริ่มต้นจากชีต

| | |
|---|---|
| เงินสดที่ใช้ได้ | ฿124,491 (สินทรัพย์ ฿128,491 ไม่นับลูกหนี้ ฿4,000) |
| หนี้สินรวม | ฿0 |
| ต้องจ่าย/ตั้งงบไว้ต่อเดือน | ฿52,369 — จ่ายประจำ 10 รายการ ฿37,419 · มีงบไว้ใช้ 7 รายการ ฿14,950 |
| สิ้นเดือนจะเหลือ | ฿72,122 |

## วิธีใช้บนมือถือ

### วิธีที่ 1 — GitHub Pages (แนะนำ)

URL ของแอป: **https://hardza1230.github.io/CongratePrim/**

workflow จะเปิด Pages ให้เองในการรันครั้งแรก ไม่ต้องไปตั้งค่าอะไร

ถ้าการรันครั้งแรกล้มที่ขั้นตอน *Configure Pages* (บางบัญชีไม่ยอมให้เปิดผ่าน API) ให้เปิดเองครั้งเดียว:
**Settings → Pages → Build and deployment → Source** เลือก **GitHub Actions** แล้วสั่งรัน workflow ใหม่

บนมือถือ:

- **iPhone (Safari)** เปิด URL → ปุ่มแชร์ → เพิ่มไปยังหน้าจอโฮม
- **Android (Chrome)** เปิด URL → เมนู ⋮ → ติดตั้งแอป

เปิดจากไอคอนบนหน้าจอโฮมจะได้เต็มจอไม่มีแถบเบราว์เซอร์ และใช้ได้แม้ไม่มีเน็ต

### วิธีที่ 2 — APK สำหรับ Android

ไปที่หน้า **Releases** ของ repo โหลดไฟล์ `.apk` ตัวล่าสุดจากมือถือแล้วกดติดตั้ง
(ต้องอนุญาต "ติดตั้งแอปจากแหล่งที่ไม่รู้จัก" ให้เบราว์เซอร์ที่ใช้โหลด)

APK เป็น **ตัวหุ้มบาง ๆ ที่โหลดหน้าเว็บจาก Pages** ไม่ได้ฝังไฟล์เว็บไว้ข้างใน แปลว่า

- แก้โค้ดแล้ว push ขึ้น `main` → Pages อัปเดต → **แอปในเครื่องได้ของใหม่เองตอนเปิดครั้งถัดไป ไม่ต้องลง APK ใหม่**
- เปิดครั้งแรกต้องมีเน็ตหนึ่งครั้ง หลังจากนั้น service worker ทำให้เปิดออฟไลน์ได้
- ข้อมูลใน APK แยกคนละที่กับข้อมูลในเบราว์เซอร์ (คนละ localStorage)

### วิธีที่ 3 — เสิร์ฟจากคอมในบ้าน

`python3 -m http.server 8000` แล้วเปิด `http://<ไอพีคอม>:8000` จากมือถือในไวไฟเดียวกัน
(เปิดจาก `file://` ตรง ๆ ไม่ได้ เพราะ service worker จะไม่ทำงาน)

## GitHub Actions ที่มีอยู่

| workflow | รันเมื่อไหร่ | ทำอะไร |
|---|---|---|
| `pages.yml` | ทุก push ขึ้น `main` | deploy เว็บขึ้น GitHub Pages |
| `android.yml` | แก้โฟลเดอร์ `android/` หรือกดรันเอง | build APK แล้วออก GitHub Release ใหม่ |

`android.yml` ไม่รันทุก push เพราะตัวหุ้ม APK แทบไม่ต้องเปลี่ยน — เนื้อหาอัปเดตผ่าน Pages อยู่แล้ว
ถ้าอยากได้ APK ตัวใหม่เมื่อไหร่ก็เข้า Actions → *Build Android APK* → **Run workflow**

### ลายเซ็นของ APK

ถ้าไม่ได้ตั้งค่าอะไร CI จะสร้างกุญแจเซ็นใหม่ทุกครั้งที่ build แปลว่า**ถ้าลง APK ตัวใหม่ทับตัวเก่าจะขึ้น error
ต้องถอนแอปเดิมออกก่อน** (ข้อมูลในแอปจะหายไปด้วย)

อยากให้ลายเซ็นคงที่และอัปเดตทับได้ ให้สร้างกุญแจเก็บไว้เป็น secret:

```bash
keytool -genkeypair -v -keystore gobsong.jks -alias gobsong \
  -keyalg RSA -keysize 2048 -validity 10000 \
  -storepass <รหัสที่ตั้งเอง> -keypass <รหัสที่ตั้งเอง> \
  -dname "CN=Gob Song, O=Personal, C=TH"

base64 -w0 gobsong.jks    # เอาผลลัพธ์ไปใส่ secret
```

แล้วไปที่ **Settings → Secrets and variables → Actions** เพิ่ม 4 ตัว:
`ANDROID_KEYSTORE_BASE64`, `ANDROID_KEYSTORE_PASSWORD`, `ANDROID_KEY_ALIAS`, `ANDROID_KEY_PASSWORD`

⚠️ เก็บไฟล์ `.jks` ไว้ให้ดีและอย่า commit ลง repo (repo นี้เป็น public) — `.gitignore` กันไว้ให้แล้ว

## โครงสร้างไฟล์

```
index.html                  แอปทั้งหมด (HTML + CSS + JS + ข้อมูลเริ่มต้น) ในไฟล์เดียว
manifest.webmanifest        ข้อมูลแอปสำหรับติดตั้งบนหน้าจอโฮม
sw.js                       service worker แคชแบบ cache-first ให้เปิดออฟไลน์ได้
icon.svg                    ไอคอนแอป
android/                    โปรเจกต์ Android ตัวหุ้ม (WebView ที่โหลด URL ของ Pages)
.github/workflows/          workflow deploy Pages และ build APK
```

## ข้อควรทราบเรื่องข้อมูล

ข้อมูลทั้งหมดเก็บใน `localStorage` ของเบราว์เซอร์บนเครื่องนั้น ๆ

- ไม่ถูกส่งออกไปไหน ไม่มีเซิร์ฟเวอร์หลังบ้าน
- **ไม่ซิงก์ข้ามเครื่อง** — จ๋ากับฮาร์ทเปิดคนละเครื่องจะเห็นข้อมูลแยกกัน
- ถ้าล้างข้อมูลเบราว์เซอร์ ข้อมูลจะหาย และต้องเริ่มจากค่าเริ่มต้นใหม่

ปุ่ม **รีเซ็ตกลับค่าเริ่มต้นจากชีต** อยู่ที่หน้า 🏦 บัญชี ล่างสุด
